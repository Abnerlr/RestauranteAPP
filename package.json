{
  "name": "restaurante-app",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready SaaS platform for restaurant customer service",
  "scripts": {
    "dev": "turbo run dev",
    "dev:backend": "pnpm -C backend dev",
    "dev:web": "pnpm -C apps/web dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
